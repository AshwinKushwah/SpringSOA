({
	doInit : function(component, event, helper) {
	    /*
        var action = component.get('c.getAppName');	
        action.setCallback(this,function(response){
            var state = response.getState();
            if(state === 'SUCCESS'){
                var currentAppName = response.getReturnValue();
                console.log("app name is : , ", currentAppName);
                //component.set("v.appName", currentAppName);
            }
        }); 
        $A.enqueueAction(action); */
        //component.set("v.appName", $CurrentPage);
        
	}
})